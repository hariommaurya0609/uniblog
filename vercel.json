{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma db push && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/scrape",
      "schedule": "0 */1 * * *"
    }
  ]
}
